/**
 * API client calls.
 * TODO: Axios/fetch wrapper with base URL from env. Auth token in headers.
 *       Methods: login, signup, uploadSyllabus, parseSyllabus, getCourses,
 *       generateSchedule, exportCalendar. Used by pages and hooks.
 */

export default {};
