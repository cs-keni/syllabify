/**
 * Schedule preview component.
 * TODO: Display proposed time blocks (list/calendar). Approve or reject.
 *       Call api client to generate, then export. Used by Dashboard, Upload flow.
 */
export default function SchedulePreview() {
  return null;
}
