/**
 * Syllabus upload component.
 * TODO: File upload (PDF) + paste plain text. Call api client upload/parse.
 *       Show extracted data for review/edit before confirm. Used by Upload page.
 */
export default function SyllabusUpload() {
  return null;
}
