/**
 * Course card component.
 * TODO: Show single course (name, term, assignment count). Link to edit/view.
 *       Used in Dashboard, course list UIs.
 */
export default function CourseCard() {
  return null;
}
