/**
 * Main App component.
 * TODO: Router setup (Dashboard, Upload, Login). Auth guard for protected
 *       routes. Layout (nav, outlet). Import styles/index.css.
 */
export default function App() {
  return null;
}
