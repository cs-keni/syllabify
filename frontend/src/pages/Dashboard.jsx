/**
 * Dashboard page.
 * TODO: List user courses (CourseCard), quick links to upload, view schedules.
 *       Use useAuth for current user. Route as / or /dashboard.
 */
export default function Dashboard() {
  return null;
}
