# Software Design Specification (SDS)

- **Purpose**: Describe architecture, components, and interfaces.
- **TODO**: System architecture (client–server, backend modules, DB schema). API contracts (auth, syllabus, schedule, export). Data flow for parse → review → schedule → export. 3NF schema (user, course, assignment, schedule).
