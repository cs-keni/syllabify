# Backend dependencies (Docker). Install via backend image / requirements-dev for tests/lint.

flask>=3.0.0
python-dotenv>=1.0.0

# Database. mysql.connector for MySQL/Railway; psycopg2 for PostgreSQL/Supabase.
# connection.py auto-detects based on DATABASE_URL env var.
sqlalchemy>=2.0.0
mysql-connector-python>=8.0.0
pymysql>=1.1.0
psycopg2-binary>=2.9.0

# Auth (JWT, password hashing)
pyjwt>=2.8.0
bcrypt>=4.0.0

# Google OAuth and Calendar API
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0

# PDF / text / DOCX (syllabus parsing)
pypdf>=3.0.0
pdfplumber>=0.10.0

# LLM (hybrid parser - OpenAI)
openai>=1.0.0
python-dateutil>=2.8.0
python-docx>=1.0.0

# CORS
flask-cors>=4.0.0

# Rate limiting (brute-force protection on auth endpoints)
flask-limiter>=3.5.0

# Production WSGI (Render, etc.)
gunicorn>=21.0.0

# ICS / iCal parsing
icalendar>=5.0.0
requests>=2.31.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
