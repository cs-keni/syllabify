# DB initialization.
# TODO: Create tables from models (base.metadata.create_all). Optionally
#       seed minimal data. Run via `python -m app.db.init_db` or Docker.
#
# DISCLAIMER: Project structure may change. This describes the general idea.
